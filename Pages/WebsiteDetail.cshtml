@page
@model SatraWebApplication.Pages.WebsiteDetailModel
@{
}
<p class="alert alert-primary">
آدرس رسانه: <a  target="_blank" href="@Model.WebsiteInfo.URL"> @Model.WebsiteInfo.URL </a></p>

@* @Html.Raw(@Model.CurrentState) *@
<div id="LastState">
    <div class="spinner-border text-primary" role="status">
        <span class="sr-only">Loading...</span>
    </div>
</div>
@* @if (Model.HasScreenShot)
{
    <p class="alert alert-info">
        <a target="_blank" href=@($"screenshot/{ Model.LastScreenShotImage}.png")>تصویر آخرین بررسی وضعیت</a>

    </p>  
}
 *@
<br />
@{
    int index = 1; // Start index from 1
}

<table class="table table-bordered">
    <thead>
        <tr>
            <th style="width:50px">
                ردیف
            </th>
            <th>
               نتیجه
            </th>
            <th>تاریخ و ساعت </th>
           
            <th>تصویر </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WebsiteInfo.WebSiteUpTimes.OrderByDescending(t=>t.ResultDate).Take(30))
{
    <tr>
                        <td>
                            @index
                        </td>
                        <td>
                            @item.Result


                        </td>
                        <td>
                            @item.ResultDate


                        </td>
                        <td>
                        @if (Model.ScreenShotExist(item.ID))
                        {
                              <a target="_blank" href=@($"screenshot/{ item.ID.ToString()}.png")>مشاهده</a>
                        }
                            


                        </td>
                        </tr>

    index++;
}
    </tbody>
</table>
@section Scripts {
    <script>
        // Load the data asynchronously after the page is ready
        async function loadSalesData() {
            try {
                
                const response = await fetch('/api/WSStatus/status?id='+@Model.WebsiteInfo.ID.ToString()); // Call an API endpoint
                const data = await response.json();

                // Update the DOM with the data
                const container = document.getElementById('LastState');
                container.innerHTML = ` ${data.lastStatus}`;
                //alert(`<p>Total Sales: ${data.totalAmount}</p>`);
            } catch (error) {
                console.error('Error loading data:', error);
            }
        }

        // Trigger the load
        loadSalesData();
    </script>
}
